# Azure VM frontend environment template.
# Replace the Application Gateway public URL/IP if it changes.
# This must point at Application Gateway, not the backend VM directly.

NODE_ENV=production
NEXT_PUBLIC_API_BASE_URL=http://<APP_GATEWAY_PUBLIC_HOST>
