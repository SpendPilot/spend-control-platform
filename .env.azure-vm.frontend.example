# Azure VM frontend environment template.
# Current Application Gateway public IP: 20.207.66.136
# Keep this empty to use same-origin /api routing through Application Gateway.

NODE_ENV=development
NEXT_PUBLIC_API_BASE_URL=
